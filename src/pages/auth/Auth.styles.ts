import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
	form: {
		textAlign: "start",
		margin: "10px",
	},

	mainBtn: {
		width: "100%",
		borderRadius: "20px",
	},
}));

export default useStyles;
